import { Timestamp } from 'rxjs/internal/operators/timestamp';

export interface resenia {
    categoria: string;
    fecha: Date;
    imagen: string;
    puntuacion: number;
    resenia: string;
    nombre: string;
}